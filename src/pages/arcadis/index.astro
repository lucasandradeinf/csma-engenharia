---
/**
 * DIREÇÃO 02 ("arcadis") — rota histórica.
 *
 * Mantida para não quebrar os links já enviados ao cliente e a publicação
 * atual no GitHub Pages (`/csma-engenharia/arcadis/`). Renderiza exatamente
 * a mesma página que a raiz `/`, mas continua `noindex` para que só a raiz
 * seja indexada.
 */
import Pagina from '../../variants/arcadis/Pagina.astro';
---

<Pagina />
